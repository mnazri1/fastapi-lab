services:
  db:
    image: postgres:15-alpine
    container_name: shop_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secretpassword
      POSTGRES_DB: computer_shop_db